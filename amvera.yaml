meta:
  environment: python
  toolchain:
    name: python
    version: "3.10"
build:
  dockerfile: Dockerfile
run:
  persistenceMount: /data
  containerPort: 8000
  env:
    - name: DATA_DIR
      value: /data
